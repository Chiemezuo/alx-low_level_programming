#include "main.h"

/**
 *_abs - Check main.h
 * @r: integer input
 * Description: Returns absolute value of a number
 * Return: Absolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
